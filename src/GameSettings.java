/**
 * @author 315009449.
 * @version 1.0
 * @since 2020-04-24
 */


public interface GameSettings {
    /**
     * @param g is the game we want to add the Sprites && Collidables to.
     */
    void addToGame(GameLevel g);
}
